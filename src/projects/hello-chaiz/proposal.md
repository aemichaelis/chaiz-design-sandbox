# Hello Chaiz — proposal

## Pushback before solution

None — this is a setup verification, not a real product ticket.

## Solution shape

One vertical page with four stacked sections:

1. **Semantic colors** — 4 swatches, grid at md+, verifies bg/fg/primary/muted mapping
2. **Glow accent** — 10-step palette in a responsive grid (5 cols → 10 cols at md)
3. **Buttons** — four variants side-by-side to sanity-check hover states
4. **Typography** — size scale with semibold headings

Poppins loaded via Google Fonts; deep teal (#042f2e) as foreground confirms tokens wired correctly.

## What this does NOT cover

Dark mode toggle, interactions, real-world components (forms, modals, etc.). Those show up in real tickets.
